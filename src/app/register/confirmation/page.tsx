import Link from "next/link";
// eslint-disable-next-line @typescript-eslint/no-unused-vars
type PageProps<T extends string> = { searchParams: Promise<Record<string, string | string[] | undefined>> };
export const metadata={title:"You’re in! | Provo Tennis"};
export default async function Page({searchParams}:PageProps<"/register/confirmation">){const p=await searchParams;const waitlisted=p.status==="waitlisted";return <main className="confirmation"><div className="eyebrow centered">Registration received</div><h1>{waitlisted?"You’re on the list!":"You’re in! 🎾"}</h1><p className="lead">{waitlisted?"We’ll contact you as soon as a spot opens.":"You’re registered for the Provo Labor Day Tennis Tournament."}</p><div className="confirmation-box"><div className="confirmation-row"><b>Name</b><span>{p.name}</span></div><div className="confirmation-row"><b>Date</b><span>Monday, September 7, 2026</span></div><div className="confirmation-row"><b>Venue</b><span>Provo · exact courts coming soon</span></div><div className="confirmation-row"><b>Status</b><span>{waitlisted?"Waitlisted":"Registered"}</span></div>{waitlisted&&p.position&&<div className="confirmation-row"><b>Waitlist position</b><span>#{p.position}</span></div>}<div className="confirmation-row"><b>Registration #</b><span>{p.id}</span></div><div className="confirmation-row"><b>Entry fee</b><span>Price coming soon</span></div></div><p>We’ll email final venue, match time, and payment details once confirmed.</p><div className="submit-row centered"><a className="button dark" href="/api/calendar">Add to calendar</a><Link className="button" href="/">Back home</Link></div></main>}
