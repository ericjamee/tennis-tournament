import { createSupabaseServer } from "@/lib/supabase-server";
export async function GET(){const db=await createSupabaseServer();const{data:{user}}=await db.auth.getUser();if(!user)return new Response("Unauthorized",{status:401});const{data}=await db.from("registrations").select("first_name,last_name,email,phone,age,self_rating,ntrp,utr,years_playing,experience,preferred_division,notes,status,waitlist_position,seed,created_at").order("created_at");const rows=data||[];const keys=rows.length?Object.keys(rows[0]):[];const esc=(v:unknown)=>`"${String(v??"").replaceAll('"','""')}"`;const csv=[keys.join(","),...rows.map(r=>keys.map(k=>esc(r[k as keyof typeof r])).join(","))].join("\n");return new Response(csv,{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":"attachment; filename=provo-tennis-registrations.csv","Cache-Control":"private, no-store"}})}
